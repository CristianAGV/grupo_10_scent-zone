// en este archivo solo vamos a requerir los modelos tanto de productos como de usuarios que creemos en esta carpeta cuando tengamos la bas de datos tipo JSON //
const productsModel = require('./productsModel-old');

module.exports = {productsModel };